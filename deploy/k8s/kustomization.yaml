# Thin entrypoint so `kubectl kustomize deploy/k8s` keeps working; the actual
# manifests live in base/ (and overlays/ for variants such as RPO=0).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./base
